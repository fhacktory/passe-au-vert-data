


# Pb: on peut avoir plusieurs périodes rouge/vert qui fonctionnent pour un jeu de données (une infinité en faite!).
# Il faut introduire une forme de prior.
# Max(t_red, t_green) < 200 et Min(t_red, t_green) > 15
